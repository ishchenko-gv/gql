import { Router } from "express";
// import fileStorage from "../../../config/file-storage";

const router = Router();

// router.post("/upload-avatar", fileStorage.single("test"), (req, res) => {
//   console.log(req);
//   res.status(200).send();
// });

export default router;
